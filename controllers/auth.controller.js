export {
  googleCallback as googleCallbackController,
  googleLogin as googleStartController,
  login as loginController,
  me as meController,
  register as registerController,
  requireAuth,
} from "./auth.controller.google.js";